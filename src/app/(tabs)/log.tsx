/**
 * Placeholder route for the tab bar's center "+" slot. Its custom tab button
 * opens the log sheet instead of navigating, so this screen never renders.
 */
export default function LogPlaceholder() {
  return null;
}
